export const PIXELS_PER_SECOND = 20;

/** Vertical row height for each timeline track (clamped when the panel is resized). */
export const TIMELINE_TRACK_HEIGHT_MIN = 36;
export const TIMELINE_TRACK_HEIGHT_MAX = 120;

/** Horizontal snap: pointer must be within this many pixels of a snap time. */
export const TIMELINE_SNAP_THRESHOLD_PX = 12;

/** Toolbar (h-10) + ruler row (h-6); subtract from timeline root height for track stack area. */
export const TIMELINE_ABOVE_TRACKS_CHROME_PX = 64;
